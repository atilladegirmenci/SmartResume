# Copilot Instructions

## Project Guidelines
- User prefers a simpler Solution Explorer structure: avoid a broad top-level `Components` folder when feature files belong under `Pages`, and prefers organizing CSS into clearer folder structure when possible.